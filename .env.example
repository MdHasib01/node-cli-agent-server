PORT=3000
DEFAULT_CLI=agy
# "default" means no --model flag is passed, so the CLI uses its own default model.
DEFAULT_MODEL=default
CLI_TIMEOUT=180000
# Optional. When set, /api and /outputs require "Authorization: Bearer <key>" or "x-api-key: <key>".
API_KEY=
